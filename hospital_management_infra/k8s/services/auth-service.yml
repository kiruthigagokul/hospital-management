apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: hospital

spec:
  replicas: 1

  selector:
    matchLabels:
      app: auth-service

  template:
    metadata:
      labels:
        app: auth-service

    spec:
      containers:
      - name: auth-service
        image: 897355252117.dkr.ecr.ap-south-1.amazonaws.com/auth-service:v1

        ports:
        - containerPort: 8081

        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:mysql://mysql:3306/hospital

        - name: SPRING_DATASOURCE_USERNAME
          value: root

        - name: SPRING_DATASOURCE_PASSWORD
          value: root


---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: hospital

spec:
  selector:
    app: auth-service

  ports:
  - port: 8081
    targetPort: 8081