apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient-service
  namespace: hospital

spec:
  replicas: 1

  selector:
    matchLabels:
      app: patient-service

  template:
    metadata:
      labels:
        app: patient-service

    spec:
      containers:
      - name: patient-service
        image: 897355252117.dkr.ecr.ap-south-1.amazonaws.com/patient-service:v1

        ports:
        - containerPort: 8082

        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:mysql://mysql:3306/hospital

        - name: SPRING_DATASOURCE_USERNAME
          value: root

        - name: SPRING_DATASOURCE_PASSWORD
          value: root


---
apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: hospital

spec:
  selector:
    app: patient-service

  ports:
  - port: 8082
    targetPort: 8082