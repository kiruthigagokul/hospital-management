apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  labels:
    app: billing-service

spec:
  replicas: 1

  selector:
    matchLabels:
      app: billing-service

  template:
    metadata:
      labels:
        app: billing-service

    spec:
      containers:

        - name: billing-service

          image: "{{ .Values.registry }}/{{ index .Values.services "billing-service" "image" }}:{{ index .Values.services "billing-service" "tag" }}"

          ports:
            - containerPort: {{ index .Values.services "billing-service" "port" }}

          envFrom:
            - secretRef:
                name: hospital-db-secret

---

apiVersion: v1
kind: Service
metadata:
  name: billing-service

spec:
  selector:
    app: billing-service

  ports:
    - port: {{ index .Values.services "billing-service" "port" }}
      targetPort: {{ index .Values.services "billing-service" "port" }}

  type: ClusterIP